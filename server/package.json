{
  "name": "server",
  "version": "1.0.0",
  "description": "Server-side",
  "main": "main.js",
  "scripts": {
    "dev": "nodemon main.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "*",
    "express": "*",
    "mongoose": "*",
    "morgan": "*",
    "nodemon": "*",
    "passport": "*"
  }
}
