{
  "name": "react-separated-client-server-boilerplate",
  "version": "1.0.0",
  "description": "React with client and server side separated boilerplate",
  "main": "main.js",
  "scripts": {
    "start": "(cd client && npm start) && (cd server && npm run dev)",
    "update": "(cd client && npm i) && (cd ../server && npm i)",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Guilherme Jacob",
  "license": "ISC"
}
